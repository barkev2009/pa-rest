1. POST /auth               (авторизация)                           схема есть
2. GET /tasks          (получение списка задач)                схема есть
3. GET /task           (получение данных по задаче)            схема есть
4. POST /set_task_status    (присвоение статус задаче)
5. GET /active_users   (получение активных пользователей)
6. GET /collateral     (получение данные по обеспечению)

ПОДУМАТЬ:
- разделять ли на разные запросы или нет
- нужна ли локаль?
- сортировка и фильтрация будет на стороне клиента?