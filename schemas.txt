1. POST /auth               (авторизация)                           схема есть
2. POST /get_tasks          (получение списка задач)                схема есть
3. POST /get_task           (получение данных по задаче)            схема есть
4. POST /set_task_status    (присвоение статус задаче)
5. POST /get_active_users   (получение активных пользователей)
6. POST /get_collateral     (получение данные по обеспечению)

ПОДУМАТЬ:
- разделять ли на разные запросы или нет
- нужна ли локаль?
- сортировка и фильтрация будет на стороне клиента?